# 插件兼容性承诺

`Vortex.Plugin.Abstractions` 是插件唯一允许引用的 Vortex 程序集，当前主版本为 1。

主体更新时必须保留主版本 1 的程序集标识、公开类型、成员签名和既有语义。新增能力只允许以新增成员或新的可选接口方式发布，不能修改或删除现有成员。只要插件继续引用本程序集的 1.x API，主体内部对命令、数据库、协议或服务的重构不会要求重新编译插件。

发生无法兼容的设计变更时，必须发布新的主版本并让宿主同时保留 1.x 契约的适配实现；不得以替换 1.x API 的方式破坏已发布插件。
